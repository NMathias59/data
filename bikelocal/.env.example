# Example .env file for LocalBike dbt project
# Copy this to `.env` and fill values for your local environment.

# ClickHouse connection
CLICKHOUSE_HOST=host.docker.internal
CLICKHOUSE_PORT=8123
CLICKHOUSE_USER=default
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DATABASE=localbike_raw

# dbt settings
DBT_TARGET=clickhouse
DBT_PROFILES_DIR=${PWD}/

# Optional: other secrets or keys
# SENTRY_DSN=
# REDIS_URL=
